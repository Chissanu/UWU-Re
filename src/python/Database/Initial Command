# To create drink_tables table in Postgres
create table drink_tables(drinkName VARCHAR(255),drinkID uuid NOT NULL DEFAULT uuid_generate_v4(),drinkList INT[6]);

# Log in as Postgres
psql -U postgres

# Log in as admin to UWURe DB
psql -U admin -d uwure


# Admin login password
uwure